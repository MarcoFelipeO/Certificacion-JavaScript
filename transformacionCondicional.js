/* Escribe tu código aquí */
function transformacionCondicional(frase){
    if(frase.charAt(0) === "a"  ){
        return frase.toUpperCase();
    }
    return frase.toLowerCase();
}


console.log(transformacionCondicional("ahora vamos a ver"));
/* Fin */